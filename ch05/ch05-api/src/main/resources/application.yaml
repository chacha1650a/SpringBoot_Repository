spring:
  application:
    name: ch05-api
  profiles:
    include: secret

mybatis:
  mapper-locations: classpath:mapper/**/*.xml
  type-aliases-package: com.korit.ch05api.entity
  configuration:
    map-underscore-to-camel-case: true
    jdbc-type-for-null: 'NULL'

db:
  init-status:
    - roles: false

app:
  oauth2:
    redirect-uri: http://localhost:5173/auth/oauth2/callback
